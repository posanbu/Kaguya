{
  "name": "@kaguya/api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "start": "node dist/server.js",
    "dev": "pnpm --filter @kaguya/api... build && tsx src/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/rate-limit": "^11.1.0",
    "@fastify/swagger": "^9.8.1",
    "@kaguya/logger": "workspace:*",
    "@kaguya/schema": "workspace:*",
    "fastify": "^5.10.0"
  }
}

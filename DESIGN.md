# Kaguya WebUI 视觉系统

## 界面模式

WebUI 属于 **Operate** 模式。用户需要快速完成 Profile 管理、NapCat 配置和消息发送，视觉表达不得改变既有页面状态机、信息顺序或标准控件行为。

## 视觉方向

- 品牌基调：月夜神社与暖色纸面几何。延续文档站的橙色 Kaguya 标识与 Lucide 线性图标，不混入第二套主图标系统。
- 浅色主题：在中性珍珠白底上使用项目提供的白色透明三角纹理，配合珊瑚橙与金色；卡片保持高可读、低透明度的实体表面。
- 深色主题：使用项目提供的靛蓝织物背景与神社轮廓，辅以页面边缘的云纹和霓虹小动物；交互强调仍使用珊瑚橙，信息边界使用青色。
- 素材边界：角色立绘不进入功能界面；来源素材只用于页面背景和非信息性装饰，部署前需由项目维护者确认公开使用授权。

## 组件规则

- 卡片圆角为 12px；按钮与输入框保持较小圆角，胶囊形只用于紧凑状态控件。
- 主按钮使用珊瑚橙；青色仅用于深色主题的边界、焦点和状态提示，不作为大面积装饰色。
- 所有页面共享同一月牙品牌标识和同一套按钮、表单、错误与成功状态。
- 响应式断点沿用 800px 和 520px；窄屏改为单栏，关键触控目标至少 44px。
- 动画仅表达加载状态，并遵守 `prefers-reduced-motion`。

## 主题与无障碍

首次访问由 `prefers-color-scheme` 选择主题，用户可通过顶栏按钮手动切换，选择保存在 `localStorage`。正文与控件必须在两种主题中维持清晰对比，焦点环不能被移除；装饰层必须 `pointer-events: none`，不可承载信息或遮挡交互。
